[uwsgi]
vhost = true
socket = /tmp/codingblind.sock
venv = /var/www/codingblind/venv
chdir = /var/www/codingblind
module = app
callable = app

# [uwsgi]
# vhost = true
# #application's base folder
# base = /var/www/codingblind
# chdir = %(base)
# #python module to import
# app = codingblind
# module = %(app)

# home = %(base)/venv
# pythonpath = %(base)

# #socket file's location
# socket = /var/www/codingblind/%n.sock

# #permissions for the socket file
# chmod-socket = 666

# #the variable that holds a flask application inside the module imported at line #6
# callable = app

# #location of log files
# logto = /var/log/uwsgi/%n.log


